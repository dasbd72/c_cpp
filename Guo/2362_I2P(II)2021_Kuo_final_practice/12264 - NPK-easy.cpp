#include <bits/stdc++.h>
using namespace std;
int main() {
    int n;
    string op;
    cin >> n;
    while (n--) {
        cin >> op;
        if (op == "ENQUEUE") {
            int x;
            cin >> x;
        } else if (op == "DEQUEUE") {
        }
    }
}