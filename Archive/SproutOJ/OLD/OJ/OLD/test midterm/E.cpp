#include<stdio.h>
#include<string.h>
#include<cmath>
#include<stack>
#include<queue>
#include<vector>
using namespace std;
int N, x;
char command[5];
queue<int> que, cpush;
stack<int> sta;
int main(){
	scanf("%d",&N);
	for(int iN = 0;iN<N;iN++){
		scanf("%s",command);
		if(command == "push"){
			scanf("%d",&x);
			cpush.push(x);
		}
		else{
			
			if()
		}
	}
	
}
