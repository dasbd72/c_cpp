#include <iostream>
#include <cstdio>
#include <vector>
#include <algorithm>
#include <cmath>
#include <cstring>
using namespace std;

int main() {
	string A, B;
	int ff[500005]={};
	vector<int> ans;
	cin >> A >> B;
	for(int i = 0; i < A.size(); i++)
	
	for(int i = 0; i < ans.size(); i++) cout << ans[i] << '\n';
	return 0;
}

